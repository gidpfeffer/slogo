package model.command;
/**
 * This class extends the TreeNode abstract class to represent commands that does have effect on turtle such
 * as forward. Command classes that extend this class does need a State object as its constructor argument 
 * @author Anh
 *
 */
public abstract class TurtleCommand extends TreeNode {
}
